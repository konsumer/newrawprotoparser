{
  "name": "rawprotoparser",
  "version": "0.0.0",
  "description": "Parse binary protobuf to JSON, when you do not have access to the source SDL.",
  "main": "index.js",
  "scripts": {
    "test": "vitest --run --globals",
    "build": "emcmake cmake -B build && emmake make -C build",
    "prepublishOnly": "npm run build",
    "pretest": "npm run build"
  },
  "keywords": [],
  "author": "David Konsumer <konsumer@jetboystudio.com>",
  "license": "MIT",
  "devDependencies": {
    "vitest": "^1.4.0"
  }
}
